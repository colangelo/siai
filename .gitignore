# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# VSCode
.vscode/

# JetBrains IDEs
.idea/
*.iml

# Environment / Secrets
.env
.env.local
.env.*.local
.env.homelab
*.pem
*.key
config/setup.toml
.gitea-token

# Docker
docker-compose.override.yml

# Logs
*.log
logs/

# Backups
*.bak
*.backup
*.sql.gz
.env.backup.*
config/setup.toml.backup.*

# Terraform (if used in repos)
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# Python (if used in repos)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.gstack/
.playwright-cli/
# Harbor POC token-signing pair (generated by `just harbor-up`)
config/harbor/private_key.pem
config/harbor/root.crt
config/harbor/registry-passwd
